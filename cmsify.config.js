// import .env and assign to exported JS.

// Only runs on server.

export default {
    API_URL: "*",
    PAGES_DIRECTORY: "*",
    INDEX_FILENAME: "*",

    DATABASE_NAME: "*",
    DATABASE_PASSWORD: "*",
}